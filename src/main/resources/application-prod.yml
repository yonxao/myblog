spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog?useUnicode=true&characterEncoding=utf-8&serverTimezone=GMT%2b8
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    open-in-view: true

logging:
  level:
    root: warn
    net.xiaosaguo.myblog: info
  file: log/blog-prod.log

server:
  port: 8081
